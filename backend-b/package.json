{
  "name": "backend-b",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Hoth POC backend B — skill INJECTED at runtime from the one-JSON-string dynamic bundle",
  "scripts": {
    "dev": "flue dev --target cloudflare --port 3584",
    "build": "flue build --target cloudflare",
    "deploy": "flue build --target cloudflare && wrangler deploy --config dist/hoth_poc_backend_b/wrangler.json"
  },
  "dependencies": {
    "@cloudflare/sandbox": "0.12.3",
    "@flue/github": "1.0.0-beta.1",
    "@flue/runtime": "1.0.0-beta.9",
    "@hoth/core": "workspace:*",
    "@octokit/rest": "^22.0.1",
    "agents": "^0.14.2",
    "hono": "^4.8.3",
    "valibot": "^1.1.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.45.0",
    "@flue/cli": "1.0.0-beta.9",
    "wrangler": "^4.78.0"
  }
}
